#!/bin/bash
#!/usr/bin/env bash
# This script uses the pgrep command with the -l option to find processes containing "bash" and display their PID and name.

# pgrep: Search for processes by name.
# -f: Match against the full command line (useful for scripts).
# -l: List the PID and the name of the process.
pgrep -fl bash
