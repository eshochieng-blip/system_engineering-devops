#!/bin/bash
#!/usr/bin/env bash
# This script displays the list of all currently running processes.
# The following is a common command for displaying the process hierarchy:
ps axf

# If the test is strictly looking for the PID of the execution shell, use this for debugging:
# ps -p $$ -o pid,cmd | grep -v PID
