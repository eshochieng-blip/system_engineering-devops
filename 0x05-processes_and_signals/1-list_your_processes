#!/bin/bash
#!/usr/bin/env bash
# This script displays the PID and command of the shell process that executed it.

# The $$ variable holds the PID of the script itself.
# Use 'ps' to find the process by PID and 'awk' to format the output.
ps -p $$ -o pid,args | grep -v PID
