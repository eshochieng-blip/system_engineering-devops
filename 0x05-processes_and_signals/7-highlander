#!/bin/bash
#!/usr/bin/env bash
# Displays a message indefinitely and traps SIGTERM

# Trap must be defined BEFORE the loop starts
trap 'echo "I am invincible!!!"' SIGTERM

while true
do
    echo "To infinity and beyond"
    sleep 2
done
